[project]
name = "compliance"
version = "0.1.0"
description = "AI 기본법 대응 — 의사결정 기록 / 편차 모니터링 / HITL gate"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/compliance"]
